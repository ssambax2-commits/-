# 코로나채권 AI추천 데스크톱 파이프라인 — 빌드/실행 의존성
# (tkinter 는 표준 라이브러리이므로 별도 설치 불필요)
catboost>=1.2,<2.0
pandas>=2.0
numpy>=1.24
openpyxl>=3.1
joblib>=1.3
xlrd>=2.0              # 구형 .xls 읽기(없으면 .xlsx 변환 안내로 폴백)
customtkinter>=5.2     # 애플풍 GUI(순수 파이썬, 오프라인 exe 번들)
darkdetect>=0.8        # customtkinter 라이트/다크 감지 의존성
ttkbootstrap>=1.10     # (구 UI 폴백용, 유지)
pyinstaller>=6.0
pytest>=7.0
